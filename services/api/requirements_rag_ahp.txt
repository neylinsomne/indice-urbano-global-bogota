# Dependencias adicionales para RAG + AHP

# Core (ya instaladas)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
asyncpg>=0.29.0

# RAG - Búsqueda Semántica
sentence-transformers>=2.2.2  # Embeddings multilingües
numpy>=1.24.0

# AHP - Cálculos Matemáticos
scipy>=1.11.0  # Opcional, para métodos AHP avanzados

# LangChain (Opcional - solo para queries avanzadas)
langchain>=0.0.350
langchain-community>=0.0.10
langchain-openai>=0.0.5
openai>=1.3.0  # Requiere API key
sqlalchemy>=2.0.0  # Requerido por LangChain SQL Agent
psycopg2-binary>=2.9.9  # Driver PostgreSQL para SQLAlchemy

# Utilidades
python-dotenv>=1.0.0
